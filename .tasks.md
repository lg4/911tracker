# 911tracker task list

## DONE
- T1–T7 (see git history): function app online (linuxFxVersion root cause + RFP bundle unbrick),
  GeoJSON serialization fix (27d06cb), API validated live (T4), browser-validated map+heatmap (T5),
  correct-band basemap pyramid regenerated (ad12266), map responsiveness fixes (003efef),
  CLAUDE.md created (c48f7f6). Local Postgres path retired; Azure Table Storage is source of truth.
- **T9 — deeper basemap zoom (e545381):** pyramid extended z7→z14, maxZoom/maxNativeZoom in sync.
- **T8 — category filter chips + colored heatmap (3089182 + fixup):** unbounded per-raw-type pills
  replaced by exactly four category chips (Police/Fire/EMS/Other, keyword-classified over
  type+title) plus an "All" reset — no more half-screen chip wall. Heat now renders one
  leaflet-heat layer per visible category in its own hue, with a low `max` cap and valid
  hex+alpha gradient stops so clusters saturate through the color ramp instead of the old
  gray/desaturated blobs (leaflet-heat's zoom-based intensity dilution is why). `type` IS
  captured from the scrape end-to-end (parse.js → table entity → GeoJSON properties) — this
  also closes the FUTURE "incident-color/type filtering" item.
- **Time-window buttons (3089182):** Today / 7 days / 30 days set the date inputs and refresh in
  one click — closes the separate FUTURE time-based-filtering UI item.
- **T10 — dark mode (3089182):** dark is the DEFAULT look via pure-CSS tile inversion on local OSM
  tiles (`invert/hue-rotate/brightness`) — keeps the keyless/offline design, no second pyramid, no
  external service. Light mode flips CSS variables; choice persists per browser (localStorage).
- **T11 — multi-county Onondaga/Syracuse:** `src/config.js` now carries a per-county `sources[]`
  (oneida JSON feed + onondaga CADInet HTML feed with bbox + geocodeContext); `parseCadinet()`
  parses the IBM JSP event table (ISO-8859-1 → minute-slice+street dedup keys since there is no
  per-event ID); `pollSource()` tags every incident with its county and drops Nominatim results
  that land outside the county bbox; `geocodeCadinet()` takes an injectable fetchQuery for tests.
  API exposes `?county=` filter; web has oneida/onondaga toggle chips, legacy rows normalize to
  'oneida'. BOUNDS already covered Syracuse so no pyramid change. Regression test: pollSource.test.js.

## Notes
- web/config.js ships with placeholder REPLACE-WITH-FUNCTION-APP in the repo; CI deploy-web.yml
  sed-substitutes at build time. Live site serves the correct o911func-e10tr1 URL. NOT a bug.
- Local az identity lacks storage data-plane roles → mint SAS inline for blob uploads/SAS, never print it.
- `az webapp config linux-fx` group does not exist in this az CLI (2.90.0); use REST PUT.
- RFP bundle upload has failed silently before (vanished bundle.zip → permanent 503s); if the
  endpoint regresses to uniform 503, check deploy-functions.yml's upload job + RFP repoint first.
- **T13 — run `/impeccable` polish pass this session:** user wants an impeccable review of the app
  + its future items (multi-county, data validation, Cloudflare CDN) considered during the pass.
  Run INLINE — user flagged NOT via subagent (backend is one model run serially; a subagent blocks
  the main loop). STATUS: plugin IS loaded this session (`impeccable:impeccable` skill + agents) —
  unblocked and runnable now.

## OPEN
- **T12 — data validation (ACID-lite provenance):** guarantee scraped data == what was live at fetch
  time without strict ACID. Scope ideas: capture source checksum + fetched-at timestamp per row at
  ingest; store per-tick provenance (source URL, count, hash) in meta table so a presented feature
  traces back to a verified tick; detect drift between table state and a fresh fetch before serving;
  flag rows whose status changed after storage (statusHistory exists for this).

- **T14 — triage the "Other" bucket:** live API sample (500 rows) shows ~263 land in Other; the big
  ones map cleanly — EMS side: `ASSIST`, `EMS CALL`, breathing-problems / convulsions-seizures /
  hemorrhage-laceration / heart-problem / stroke / unconscious-fainting codes (`06Dxx`, `12Dxx`,
  `21B/C/D*`, `19Dxx`), `TRANSFER`; police side: `DISPUTE`, `LARCENY`, `HARASSMENT`, `FRAUD`,
  `BURGLARY`, `VEHICLE STOLEN`, `ORDER OF PROTECTION`, `SHOTS FIRED`, `NOISE COMPLAINT`. Expand
  CATEGORY_RX accordingly (or a code-prefix table for the numeric EMS codes); decide where the
  genuinely ambiguous ones go: `ANIMAL`, `TRAFFIC HAZARD`, `DISABLED VEHICLE`, `LOCKOUT`,
  `PREMISE CHECK`, `PROPERTY FOUND/LOST`, `INVESTIGATION`, `JUVENILE`, `NEIGHBOR PROBLEM`.
- **T15 — hide Onondaga from the map, keep ingesting:** CADInet rows carry no street/address
  context, so their points are not meaningful relative to a street or area — user wants them off
  the rendered map while ingest continues storing them. Options: default onondaga toggle OFF in
  web (persist choice) vs. exclude from heat/features entirely until geocoding improves; pick one
  and note it in CLAUDE.md.

## FUTURE
- **Cloudflare fronting as CDN:** put Cloudflare in front of the SWA origin for caching/bandwidth
  protection (tiles are 60M+ and growing with zoom levels).
- **TINC events feed:** `https://tincevents.thruway.ny.gov/tincview.aspx?zone=SY` (Syracuse zone) —
  add alongside CADInet feeds when scoping T11; probe + parse format then.

## RULES
no secrets exposure · ONE subagent/task at a time · ai-review before commit · per-task commits
with Co-Authored-By: Claude Code <noreply@anthropic.com>
