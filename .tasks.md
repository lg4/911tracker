# 911tracker task list

## DONE
- T2 (49c52c8): clean OSM basemap pyramid replaces CARTO overlay (no more "API KEY REQUIRED").
- T3 (4250de8): local Postgres → Azure Table Storage seeded & verified live
  (360 incidents partition 2026-09 + statusHistory + meta.last_poll source=local-seed).
- **T1 (this session) — FUNCTION APP IS ONLINE.** Root cause: `siteConfig.linuxFxVersion`
  was empty (`""`) on the Linux function app → no worker runtime could boot → uniform
  503s on both wwwhost and scmhost regardless of content. Fixed via REST PUT config/web
  body `{"properties":{"linuxFxVersion":"node|22"}}` (only accepted format; `node:22` etc.
  rejected as invalid). Verified persisted + restarted → endpoint returns HTTP 200.
- **Serialization bug fixed (commit 27d06cb):** v4/Kestrel host coerced object bodies via
  `.toString()` → `[object Object]` with Content-Type text/plain, so GeoJSON never reached
  the browser. Now serializes explicitly + declares application/json. Rebuilt bundle,
  uploaded to funcbundle/blob bundle.zip, repointed WEBSITE_RUN_FROM_PACKAGE at fresh SAS,
  restarted, probed → real GeoJSON FeatureCollection (360 features), CORS pinned.
- **T4 — API validated live:** `/api/incidents?since=2026-09-15&until=2026-09-18` returns
  360 real Oneida incidents (application/json), e.g. "413 ORISKANY ST WEST, UTICA".
  OPTIONS preflight + GET both return `Access-Control-Allow-Origin:
  https://salmon-smoke-0f761930f.5.azurestaticapps.net`. (ALLOWED_ORIGIN already set.)
- **T5 — Browser-validated map+heatmap:** headless chromium vs
  https://salmon-smoke-0f761930f.5.azurestaticapps.net renders real OSM tiles over Utica,
  NY + glowing incident heatmap + legend; no CARTO overlay. Screenshot /tmp/site_shot.png.
- **UNBRICK (this session):** endpoint regressed to uniform gateway 503 after prior work.
  Root cause was NOT lapsed SAS (exp was 7 days out): the RFP blob
  `github-actions-deploy/functions/bundle.zip` had vanished from that path → every cold start's
  package download failed → permanent 503, no CORS header. Fix: pushed local main (incl.
  serialization fix 27d06cb) and re-ran deploy-functions.yml twice via gh dispatches → both
  green. Verified live: HTTP 200, application/json, ACAO pinned to SWA origin, 425 features.
  Note: two pre-existing workflow runs were failing too — bundle upload step evidently not
  surviving; if it recurs check the workflow's upload job + RFP repoint.

## Notes
- web/config.js ships with placeholder REPLACE-WITH-FUNCTION-APP in the repo; CI
  deploy-web.yml sed-substitutes vars.FUNCTION_APP_NAME at build time. Live site serves
  the correct o911func-e10tr1 URL. NOT a bug.
- Local az identity lacks storage data-plane roles → mint SAS from live app setting
  AZURE_TABLES_CONNECTION_STRING inline (never printed) for blob upload/SAS.
- `az webapp config linux-fx` group does not exist in this az CLI (2.90.0); use REST PUT.

## RULES
no secrets exposure · ONE subagent/task at a time · ai-review before commit · per-task commits
with Co-Authored-By: Claude Code <noreply@anthropic.com>
- **T6 — map responsiveness fixes (this session):** web/index.html now uses a flex-column
  layout (#map fills leftover viewport height; mobile header wrap no longer clips/overlaps),
  `map.invalidateSize()` on resize/orientationchange + ResizeObserver on #map, soft
  maxBoundsViscosity 0.5 (no rubber-band voids), initial fitBounds(BOUNDS) so first paint
  shows only real basemap, .leaflet-container fill color for out-of-pyramid pan.
  Local smoke test OK (python http.server). Committed + pushed as 003efef; deploy-web ships it live.
- **CLAUDE.md created:** repo orientation doc (layout + working rules + future-improvement
  note re: incident-type layer/filter for heatmap — API already supports type= param).
  Committed + pushed as c48f7f6 (own task); content reviewed against tree, all spot-checks pass.
- **T7 — blank basemap fixed ("no tiles") [DONE, ad12266]:** root cause = committed pyramid (commit 49c52c8)
  was generated at WRONG latitude band (~69°N northern Canada), not Oneida County (~43°N).
  X bands were right; every Y band sat ~26° too far north → fitBounds(BOUNDS) requested the
  correct ~43°N rows, all missed disk → 404 on every tile → blank map. (The DevTools
  "leaflet.js.map source-map" paste is just noise — Leaflet ships no .js.map.) Live SWA
  serves /tiles fine and live index.html == committed, so it was a data problem, not deploy.
  Fix: new scripts/gen-tiles.js computes exact {z}/{x}/{y} set covering BOUNDS from free OSM
  standard tiles, reuses existing files, prunes stale ones outside the computed set. Ran once:
  removed old 699 northband tiles, wrote 562 correct-band tiles (z10 now y=[372,378] vs old
  [235,242]). Verified on-disk bands match Web-Mercator of BOUNDS. ai-review CLEAN; committed
  ad12266 (generator + regenerated pyramid) + pushed → deploy-web ships corrected basemap live.
- **T8 — incident-type layer/filter for heatmap:** web-side control to show/hide
  incident types on the heatmap. API already supports type= and each feature carries its type
  in properties. Plan: fetch once, filter client-side; per-type color coding when filtering.
- **T9 — deeper basemap zoom (requested this session):** pyramid tops out at z12 (~500m/tile);
  user wants finer detail. Extend `scripts/gen-tiles.js` ZMAX 12→14 or 15 + matching maxZoom in
  web/index.html; re-run generator (idempotent) → deploy-web. Watch repo size: SWA deploys via
  git, so estimate tile count/bytes before committing (z13 ≈ 4× z12 tiles). OSM standard tiles go
  to ~z19.
- **T10 — dark mode basemap (requested):** current pyramid is OSM standard (light). Option A:
  pre-render a dark variant (CARTO dark_matter tiles are keyless; check tms.basemaps.cartocdn.com
  usage terms) alongside light under web/tiles-dark/, with a toggle in index.html. Option B: make
  dark default per user preference ("or make dark mode by default"). Decide A vs B when scoping;
  same generator path as T9 (parametrize tile source URL + output dir in scripts/gen-tiles.js).
- **T11 — Onondaga County / Syracuse data (requested):** make this a generic multi-county
  "911tracker". New feed `https://911events.ongov.net/CADInet/app/events.jsp` (probed: returns an
  IBM/JSP HTML page, ISO-8859-1 charset, robots noindex — needs its own parser like the Oneida
  htmlFeedFallback, not the JSON shape fetchSource.js expects today). Plan: parametrize feeds per
  county, tag each incident with a county field end-to-end (table entity → GeoJSON properties),
  ingest both counties on the timer. Map already covers it: BOUNDS [[42.8,-76.6],[43.6,-75.0]]
  includes Syracuse (~43.04N, -76.15W) so no pyramid change needed. Consider a county layer toggle
  alongside T8's type filter.
- **T12 — data validation (requested):** ACID-like (not strict) guarantee that scraped data == what
  was live and hasn't changed when stored/presented. Ideas to scope: capture source checksum +
  fetched-at timestamp per row at ingest; store provenance (source URL, count, hash) in meta so a
  presented feature can be traced back to a verified tick; detect drift between table state and a
  fresh fetch before serving; flag rows whose status changed after storage (statusHistory exists).
- **FUTURE (separate): time-based filtering UI** — buttons/chips for time windows (today / 24h /
  week); API already supports since/until, mostly web-side.
- **FUTURE (separate): incident-color/type filtering** — see T8; ensure `type` is captured from the
  scrape where available and drives per-type color coding on the heatmap/legend.
- **FUTURE (separate): Cloudflare fronting as CDN** — put Cloudflare in front of the SWA origin
  for caching/bandwidth protection (matters now that tiles are 60M+ and growing with zoom/dark).

## Future feed sources (requested)
- **TINC events**: `https://tincevents.thruway.ny.gov/tincview.aspx?zone=SY` (Syracuse zone) — add
  as an additional event source alongside CADInet feeds; probe + parse format when scoping.
- **T11 — Onondaga County / Syracuse data (requested):** make this a generic multi-county
  "911tracker". New county feed `https://911events.ongov.net/CADInet/app/events.jsp` (probed:
  returns IBM/JSP HTML, ISO-8859-1 — needs its own parse like htmlFeedFallback; NOT the JSON
  shape src/ingest/fetchSource.js expects today). Plan: parametrize feed source(s) per county,
  tag each incident with a county field end-to-end (table entity prop → GeoJSON properties),
  ingest both counties on the timer. Map already covers it: BOUNDS [[42.8,-76.6],[43.6,-75.0]]
  includes Syracuse (~43.04N, -76.15W) so no pyramid change needed. Consider a county layer toggle
  alongside T8's type filter.
- **T12 — data validation (requested):** ACID-like (not strict) guarantee that scraped data == what
  was live and hasn't changed when stored/presented. Ideas: capture an integrity checksum + fetched
  timestamp per row at ingest time; store per-tick provenance (source URL, count, hash) in meta so
  a presented feature can be traced back to a verified tick; detect drift between table state and a
  fresh fetch before serving; flag rows whose status changed after storage (statusHistory already
  exists). Scope the exact guarantees when implementing.
- **FUTURE (separate, requested): time-based filtering UI** — buttons/chips to slice incidents by
  time window (today / 24h / week); API already supports since/until.
- **FUTURE (separate, requested): incident-color/type filtering** — see T8; ensure `type` is captured
  from the scrape where available and drive per-type color coding on the heatmap/legend.
- **FUTURE (separate, requested): Cloudflare fronting as CDN** — put Cloudflare in front of the SWA
  origin for caching/bandwidth protection (esp. now that tiles are 60M+ and growing with zoom/dark).

## Future feed sources (requested, not yet built)
- **TINC events**: `https://tincevents.thruway.ny.gov/tincview.aspx?zone=SY` (Syracuse zone) — add
  as an additional event source alongside CADInet feeds. Probe + parse format when scoping.
